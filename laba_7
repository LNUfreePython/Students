class Solution:
    def countElements(self,nums: List[int]) -> int:
        nums.sort()
        
        
        n=len(nums) -1
        result, i=0,1
        while i < n
            if nums[0] < nums [i] < nums [n]:
                result += 1
            i += 1
            
        return result    