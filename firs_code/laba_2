class Solution
    def majorityElement(self,nums: List[int] -> int:
        if len(nums) == 1:
            return nums [0]
            
        dict ={}
        
        for num in nums:
            if num not in dict:
                dict[num] = 1
            else:
                dict[num]+= 1
            
        count =- 1
        max_val =- 1
        for key, value in dict.items():
            if value>count:
                max_val = key
                count=value
                
        return max_val        